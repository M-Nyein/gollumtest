<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
    </tr>
</thead>
  <tbody>
    
      <tr>
        <td><%= @admin.name %></td>
        <td><%= @admin.email %></td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),edit_admin_path(@admin), :class => 'btn btn-primary btn-xs' %>
          
        </td>
      </tr>
  </tbody>
</table>